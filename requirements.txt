# Waymo GNN Trajectory Prediction - Requirements
# Optimized for RTX Pro 6000 (Ada Lovelace, 96GB VRAM)

# PyTorch with CUDA 12.4 support (compatible with Ada Lovelace GPUs)
--extra-index-url https://download.pytorch.org/whl/cu124
torch>=2.5.0
torchvision>=0.20.0
torchaudio>=2.5.0

# PyTorch Geometric (Graph Neural Networks)
torch-geometric>=2.6.0
torch-scatter
torch-sparse
torch-cluster
torch-spline-conv

# Data handling
h5py>=3.10.0
numpy>=1.26.0,<2.0.0
pandas>=2.1.0

# TensorFlow (for Waymo dataset parsing)
tensorflow>=2.15.0

# Experiment tracking
wandb>=0.16.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0
tqdm>=4.66.0

# Scientific computing
scipy>=1.11.0

# Protobuf (for Waymo dataset)
protobuf>=4.25.0

# Utilities
Pillow>=10.0.0
pyyaml>=6.0
