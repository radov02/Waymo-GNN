# Environment file for Docker Compose
# Copy this to .env and fill in your values

# Weights & Biases API Key (get from https://wandb.ai/authorize)
WANDB_API_KEY=

# GPU Configuration (comma-separated GPU indices, e.g., "0" or "0,1")
CUDA_VISIBLE_DEVICES=0

# Optional: Custom data path (default: ./data)
# DATA_PATH=./data

# Optional: Custom checkpoint path (default: ./checkpoints)
# CHECKPOINT_PATH=./checkpoints
